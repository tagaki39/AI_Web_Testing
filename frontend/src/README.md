# Frontend Source Layout

这里放前端页面、组件与状态逻辑。

建议后续按以下模块逐步实现：

- `app/`：应用入口与全局初始化
- `layouts/`：平台布局与工作台布局
- `pages/`：页面层
- `features/`：按业务域拆分的模块
- `components/`：复用组件
- `services/`：接口请求层
- `types/`：类型定义
- `hooks/`：通用 Hooks
- `assets/`：静态资源
